part of 'home.dart';

const _boxShadow = [
  BoxShadow(
    color: Colors.black12,
    blurRadius: 7,
    offset: Offset(0, -1),
  ),
];

const _boxShadowDark = [
  BoxShadow(
    color: Colors.white24,
    blurRadius: 7,
    offset: Offset(0, -1),
  ),
];
